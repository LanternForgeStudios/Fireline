// Extracted from CombatScene.ts so generation code (src/game/generation/*)
// doesn't need to import a Phaser scene module just for these two numbers.
export const WORLD_WIDTH = 1280
export const WORLD_HEIGHT = 720
